Author: Chris Zachariah

Ping (implemented in C)

Running:

gcc CLI.c -o CLI 
sudo ./CLI <hostname or IP>

This program will take in a hostname or an IP address as an argument and
try to ping the IP. Shows packets, TTL and loss.